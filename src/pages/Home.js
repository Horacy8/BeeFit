import React, { useState } from "react";
import { Box } from "@mui/system";
import Banner from "../components/Banner";

const Home = () => {
  return (
    <Box>
      <Banner />
    </Box>
  );
};

export default Home;
